# Default: spawn `codex app-server` locally via stdio.
# CODEX_BIN=codex

# Optional: connect to an already-running app-server instead.
# CODEX_APP_SERVER_URL=ws://127.0.0.1:4500
# CODEX_APP_SERVER_TOKEN=...

# Optional Python interpreter.
# PYTHON=python3
